# Milestone 2 - StormCare RAG (Track C)

This file is kept for backward compatibility.

Please follow the canonical instructions in `README.md`, which document:
- tokenizer training + artifacts
- retrieval demo (BM25)
- next-token LM smoke test + outputs under `results/`
